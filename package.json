{
  "name": "codex-remote-mvp",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c cyan,magenta \"npm run dev -w @remote/server\" \"npm run dev -w @remote/web\"",
    "build": "npm run build -w @remote/shared && npm run build -w @remote/server && npm run build -w @remote/web",
    "test": "npm run test -w @remote/server && npm run test -w @remote/web",
    "typecheck": "npm run build -w @remote/shared && npm run typecheck -w @remote/server && npm run typecheck -w @remote/web",
    "start": "npm run start -w @remote/server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "playwright": "^1.62.1",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/cli": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "@capacitor/preferences": "^8.0.1",
    "@capacitor/splash-screen": "^8.0.2",
    "@capacitor/status-bar": "^8.0.3"
  }
}
